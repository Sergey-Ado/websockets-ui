import { Client, Game, Player, Room } from '../types/types.js';

export const clients: Client[] = [];
export const players: Player[] = [];
export const rooms: Room[] = [];
export const games: Game[] = [];
